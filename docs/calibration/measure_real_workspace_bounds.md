```markdown
# Measure Real Workspace Bounds (Robot)

This document records the exact steps used to measure the physical workspace
bounds in the robot base frame and write them to `yaml/bhand.yml` under
`env.real_workspace.bounds`.

... (moved for cleanliness)

Document moved into `docs/calibration` on Feb 28, 2026.
```
